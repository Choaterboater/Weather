name: BiteCast
options:
  bundleIdPrefix: app.choatelabs
  deploymentTarget:
    iOS: "26.5"
  developmentLanguage: en
  createIntermediateGroups: true

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
    GENERATE_INFOPLIST_FILE: NO
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"

targets:
  BiteCast:
    type: application
    platform: iOS
    sources:
      - path: Sources
    # Injects the gitignored API keys (via AppConfig's optional include) into
    # every build; without this wiring, regenerating the project silently
    # dropped all secrets.
    configFiles:
      Debug: AppConfig.xcconfig
      Release: AppConfig.xcconfig
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.choatelabs.bitecast
        INFOPLIST_FILE: Sources/Support/Info.plist
        CODE_SIGN_ENTITLEMENTS: Sources/Support/BiteCast.entitlements
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "1"
        DEVELOPMENT_TEAM: RAYW3WPJ98
    dependencies:
      - target: BiteCastWidgets

  BiteCastWidgets:
    type: app-extension
    platform: iOS
    sources:
      - path: Widget
      - path: Sources/Models/WidgetSnapshot.swift
      - path: Sources/Services/WidgetSnapshotStore.swift
      - path: Sources/Views/BiteWidgetView.swift
      - path: Sources/Views/BiteCastTheme.swift
    info:
      path: Widget/Info.plist
      properties:
        CFBundleDisplayName: BiteCast
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.choatelabs.bitecast.widgets
        CODE_SIGN_ENTITLEMENTS: Widget/BiteCastWidgets.entitlements
        CODE_SIGN_STYLE: Automatic
        TARGETED_DEVICE_FAMILY: "1"
        DEVELOPMENT_TEAM: RAYW3WPJ98

  BiteCastTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests
    dependencies:
      - target: BiteCast
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.choatelabs.bitecast.tests
        GENERATE_INFOPLIST_FILE: YES

  BiteCastUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: UITests
    dependencies:
      - target: BiteCast
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.choatelabs.bitecast.uitests
        GENERATE_INFOPLIST_FILE: YES

schemes:
  BiteCast:
    build:
      targets:
        BiteCast: all
        BiteCastTests: [test]
        BiteCastUITests: [test]
    test:
      targets:
        - BiteCastTests
        - BiteCastUITests
      gatherCoverageData: true
    run:
      config: Debug
    archive:
      config: Release
